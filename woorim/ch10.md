# 객체란?

---

- 원시 값을 제외한 나머지값으로, 다양한 타입의 값을 하나의 단위로 구성한 복합적인 자료구조
- **변경 가능한 값 mutable value** (↔ 원시 타입의 값은 변경 불가능한 값 immutable value)
- 프로퍼티와 메서드로 구성된 집합체
- 객체의 프로퍼티가 될 수 있는 값 : **js에서 사용가능한 모든 값**

# 객체 프로퍼티에 접근하는 방법

---

```jsx
var person = {
	name: 'Lee'
};
```

위 코드처럼 person이라는 객체가 있고, 그 객체에는 name이라는 프로퍼티가 있으며, 그 프로퍼티의 key는 name value는 ‘Lee’이다. 

이 프로퍼티에 접근할 수 있는 방법은 마침표 표기법과 대괄호 표기법, 이 두 가지가 있다.

## 1️⃣ 마침표 표기법 dot notation

- 사용법 : `객체로 평가되는 표현식.프로퍼티 키`
- 마침표 표기법은 마침표 프로퍼티 접근 연산자인 .을 사용한다.

```jsx

console.log(person.name);
```

## 2️⃣ 대괄호 표기법 bracket notation

- 사용법 : `객체로 평가되는 표현식[프로퍼티 키]`
- 대괄호 프로퍼티 접근 연산자인 [ ] 을 사용한다.
- [ ] 안에 지정하는 프로퍼티 키는 반드시 따옴표로 감싼 문자열이어야 한다.
- **프로퍼티 키가 식별자 네이밍 규칙을 준수하지 않는 이름(=js에서 사용 가능한 유효한 이름)**이 아니면, 반드시 대괄호 표기법을 사용해야 한다.
- 단, 프로퍼티 키가 숫자로 이루어진 문자열일 경우 따옴표를 생략할 수 있다.
